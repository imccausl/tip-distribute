/* DisplayUserTipOut.jsx
 * 
 * The component responsible for displaying tip outs to individual users. Information
 * in this view includes: 
 *     tip out amount, 
 *     how much hourly, 
 *     how much money total for the week,
 *     some basic stats about the tip out (average, how it compares to previous weeks, etc.),
 *     tip out message (if a message was attached),
 *     when the tip out was completed,
 *     how many days until it expires,
 *     finally a button/checkmark to indicate whether tip out was picked up.
 */
